import { ComingSoon } from "@hife/ui";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Places — Concord, MA",
};

export default function PlacesPage() {
  return <ComingSoon slug="concord-ma" section="Places" />;
}
