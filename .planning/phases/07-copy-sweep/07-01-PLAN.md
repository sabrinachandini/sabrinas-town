---
phase: 07-copy-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/partner/page.tsx
  - web/app/about/page.tsx
  - web/app/page.tsx
autonomous: true
requirements:
  - COPY-01

must_haves:
  truths:
    - "The word 'free' does not appear in partner/page.tsx or about/page.tsx"
    - "The words 'widget' and 'embed' do not appear in partner/page.tsx or about/page.tsx"
    - "The embed iframe section is gone from the partner page"
    - "The homepage stat card reads 'Open — Always', not 'Free — Always'"
    - "The homepage teacher strip no longer says 'Free, always'"
    - "next build exits 0 after all edits"
  artifacts:
    - path: "web/app/partner/page.tsx"
      provides: "Partner page with open/stewardship language, no embed section, OpenFeature component"
      contains: "Open to everyone, always"
    - path: "web/app/about/page.tsx"
      provides: "About page without embeddable content reference"
      contains: "deeper organizational voice"
    - path: "web/app/page.tsx"
      provides: "Homepage with Open — Always stat card and no 'Free, always' in teacher strip"
      contains: "Open — Always"
  key_links:
    - from: "web/app/partner/page.tsx"
      to: "FreeFeature → OpenFeature rename"
      via: "definition at line 246, four call sites at lines 43-46"
      pattern: "OpenFeature"
    - from: "web/app/partner/page.tsx"
      to: "Embed Widget section deletion"
      via: "Remove from <Divider> before {/* Embed Widget */} through closing </section>"
      pattern: "Embed Widget"
---

<objective>
Sweep all "free" language and widget/embed references from the partner, about, and homepage pages. Replace with stewardship-grounded "open" language. Delete the embed iframe section from the partner page entirely.

Purpose: Reframe the product's value proposition away from pricing ("free") toward access philosophy ("open"), and remove embed/widget marketing that no longer reflects the partnership offering.
Output: Three edited TSX files, verified clean by grep and next build.
</objective>

<execution_context>
@/Users/sabrinachandini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sabrinachandini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-copy-sweep/07-RESEARCH.md
</context>

<interfaces>
<!-- Exact current content of the three files — no codebase exploration needed. -->

From web/app/partner/page.tsx (key lines):
```
Line 24:  profile is free and always will be. Partnership is for the towns that
Line 25:  want to go further: to shape how their story is told, to reach
Line 26:  teachers and travelers through embeddable tools, and to understand who
Line 35:  {/* What stays free */}
Line 37:  <Heading level={2}>What stays free, always</Heading>
Line 43:  <FreeFeature>Public town profiles with full historical content</FreeFeature>
Line 44:  <FreeFeature>Teacher modules — lesson plans, primary sources, quizzes</FreeFeature>
Line 45:  <FreeFeature>Network search and cross-town connections</FreeFeature>
Line 46:  <FreeFeature>Source citations and credibility tiers on every claim</FreeFeature>
Line 62:  title="Embeddable Widgets"
Line 63:  description="Add your town's profile to your website with a single line of code. Widgets show your town's score, summary, and a link back to the full profile — and update automatically as content improves."
Line 100: <span className="text-h2 font-heading font-bold">Free</span>
Lines 196-225: <Divider spacing="section" /> + {/* Embed Widget */} section + closing </section>
Line 246: function FreeFeature({ children }: { children: React.ReactNode }) {
```

From web/app/about/page.tsx (key lines):
```
Lines 119-122: analytics, custom editorial collaboration, and embeddable
               content for local tourism sites. The core history remains open
               regardless.
```

From web/app/page.tsx (key lines):
```
Line 184: { stat: "Free — Always",        detail: "No paywalls. History is for everyone." },
Line 295: Sixteen states covered. Free, always. No login required.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Apply all copy replacements across three files</name>
  <files>
    web/app/partner/page.tsx
    web/app/about/page.tsx
    web/app/page.tsx
  </files>
  <action>
Make the following targeted edits. Do NOT change anything outside these specific items.

**web/app/partner/page.tsx — 8 changes:**

1. Opening paragraph (lines 21-31): Replace the `<Text>` block content with:
   > "Every town in this network already has a public profile — scored, sourced, and connected to the broader story of the Revolution. That profile is open to everyone and always will be. Partnership is for towns that want to go further: to shape how their story is told, to reach teachers and travelers, and to understand who is engaging with their history. This isn't a claim-your-listing product. It's a stewardship model — your community's history, maintained with the same rigor we apply to every town, with your organization's voice and priorities woven in."

2. Comment at line 35: Change `{/* What stays free */}` to `{/* What stays open */}`

3. Section heading at line 37: Change `What stays free, always` to `Open to everyone, always`

4. Four `<FreeFeature>` call sites (lines 43-46): Rename to `<OpenFeature>` at all four call sites. Content of each item stays identical.

5. FeatureCard at lines 61-64 (title="Embeddable Widgets"): Replace the entire `<FeatureCard>` with:
   ```tsx
   <FeatureCard
     title="Content Integration"
     description="Bring your town's profile into your own digital presence through our partner API. Your data, your design — updated automatically as content improves."
   />
   ```

6. Price label at line 100: Change `Free` to `Open`

7. Embed Widget section deletion (lines 196-225): Delete the `<Divider spacing="section" />` that precedes `{/* Embed Widget */}` AND the entire `<section>` block from `{/* Embed Widget */}` through its closing `</section>` tag. The `<Divider spacing="section" />` and `{/* CTA */}` section that follow should remain. Be careful: after deletion the CTA section's preceding Divider (currently line 225) becomes the correct separator — do not remove it.

8. Component rename at line 246: Change `function FreeFeature(` to `function OpenFeature(`. The implementation body stays identical.

**web/app/about/page.tsx — 1 change:**

Lines 119-122 inside the "For towns and organizations" block: Replace:
   > `analytics, custom editorial collaboration, and embeddable content for local tourism sites.`

With:
   > `analytics, custom editorial collaboration, and a deeper organizational voice in how their town's story is told.`

Full paragraph after edit should read: "Every town in the network has a researched public profile at no cost — events, people, places, stories, and sources. Communities that want a deeper presence can join the partnership program, which adds analytics, custom editorial collaboration, and a deeper organizational voice in how their town's story is told. The core history remains open regardless."

**web/app/page.tsx — 2 changes:**

Line 184 stat card: Change:
   `{ stat: "Free — Always", detail: "No paywalls. History is for everyone." }`
To:
   `{ stat: "Open — Always", detail: "No paywalls. No login required." }`

Line 295 teacher strip paragraph: Change:
   `Sixteen states covered. Free, always. No login required.`
To:
   `Sixteen states covered. No login required.`

**Out of scope — do NOT touch:**
- `web/app/terms/page.tsx` (legal document)
- `web/app/org/[slug]/dashboard/page.tsx` (internal admin UI)
- `web/app/embed/[slug]/route.ts` (backend implementation)
- `web/app/teach/virginia/page.tsx` ("freedom" = historical context, not product copy)
  </action>
  <verify>
    <automated>
      grep -ni "free" /Users/sabrinachandini/sabrinas-town/web/app/partner/page.tsx /Users/sabrinachandini/sabrinas-town/web/app/about/page.tsx && echo "FAIL: 'free' still present" || echo "PASS: no 'free' in partner/about"
      grep -ni "widget\|embed" /Users/sabrinachandini/sabrinas-town/web/app/partner/page.tsx /Users/sabrinachandini/sabrinas-town/web/app/about/page.tsx && echo "FAIL: widget/embed still present" || echo "PASS: no widget/embed in partner/about"
      grep -n "Open — Always" /Users/sabrinachandini/sabrinas-town/web/app/page.tsx && echo "PASS: homepage stat updated" || echo "FAIL: homepage stat not updated"
      grep -n "OpenFeature" /Users/sabrinachandini/sabrinas-town/web/app/partner/page.tsx | wc -l | xargs -I{} sh -c 'if [ {} -eq 5 ]; then echo "PASS: OpenFeature has 5 occurrences (1 def + 4 calls)"; else echo "FAIL: OpenFeature count wrong: {}"; fi'
    </automated>
  </verify>
  <done>
    - grep -ni "free" on partner/page.tsx and about/page.tsx returns 0 matches
    - grep -ni "widget|embed" on partner/page.tsx and about/page.tsx returns 0 matches
    - homepage stat card shows "Open — Always"
    - OpenFeature has exactly 5 occurrences in partner/page.tsx (1 definition + 4 call sites)
    - No orphaned JSX tags from the embed section deletion
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify next build passes</name>
  <files></files>
  <action>
Run next build from the web/ directory to confirm TypeScript is clean and no orphaned JSX tags exist from the embed section deletion. This is the final gate — all edits must be complete before running.

If build fails with a JSX parse error, the embed section deletion likely left an unclosed tag. Re-read partner/page.tsx around the deleted block and fix the unclosed element.

If build fails with a TypeScript error, check that FreeFeature was renamed at both the definition (line 246) and all four call sites (lines 43-46) — a mismatch between call sites and definition causes an "undefined component" error.
  </action>
  <verify>
    <automated>cd /Users/sabrinachandini/sabrinas-town/web && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>next build exits 0. No TypeScript errors. No JSX parse errors.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification suite:

```bash
# 1. No "free" in partner/about (case-insensitive)
grep -ni "free" /Users/sabrinachandini/sabrinas-town/web/app/partner/page.tsx /Users/sabrinachandini/sabrinas-town/web/app/about/page.tsx

# 2. No widget/embed in partner/about
grep -ni "widget\|embed" /Users/sabrinachandini/sabrinas-town/web/app/partner/page.tsx /Users/sabrinachandini/sabrinas-town/web/app/about/page.tsx

# 3. Homepage stat card updated
grep -n "Open — Always" /Users/sabrinachandini/sabrinas-town/web/app/page.tsx

# 4. Build clean
cd /Users/sabrinachandini/sabrinas-town/web && npx next build
```

Expected: Commands 1 and 2 return no matches. Command 3 returns the stat card line. Command 4 exits 0.
</verification>

<success_criteria>
1. grep -ni "free" on partner/page.tsx and about/page.tsx returns 0 results
2. grep -ni "widget|embed" on partner/page.tsx and about/page.tsx returns 0 results
3. Homepage stat card reads "Open — Always" (not "Free — Always")
4. Homepage teacher strip no longer contains "Free, always"
5. next build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-copy-sweep/07-01-SUMMARY.md` with:
- What was changed (file-by-file summary)
- Verification command outputs
- Any deviations from the plan and why
</output>
